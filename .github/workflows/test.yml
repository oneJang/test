name: test
on:
  push:
    branches: [ "master" ]
jobs:
  checkout:
    runs-on: ubuntu-latest
    steps:
    - name: sourcecode CheckOut
      uses: actions/checkout@v3
    - name: go to user-service
      run: cd user-service/ && pwd $$ ls -al
    - name: build
      run: ./gradlew build

